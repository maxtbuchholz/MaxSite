export function headerPositionIconTo(page) {
    // const locationRect = document.getElementById("locationRect");
    // alert(locationRect.style.innerHeight)
    // locationRect.style.width = `${locationRect.style.innerHeight}px`;
    // const pageRect = document.getElementById(page);
    // locationRect.style.right = `${pageRect.style.offset.right}px`;
    // locationRect.style.left = `${pageRect.left}px`;
    // locationRect.style.position = 
}